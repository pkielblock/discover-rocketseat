// Object

const person = {
    name: 'Pedro',
    age: 19,
    weight: 85,
    isAdmin: true
};

// Imprime objeto inteiro
console.log(person);

// Imprime apenas a propriedade declarada
console.log(person.name + ' tem ' + person.age + ' anos, pesa ' + person.weight + 'kg e tem o status de administrador como: ' + person.isAdmin);
